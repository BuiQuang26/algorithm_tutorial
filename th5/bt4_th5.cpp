#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n = 6, m = 15;
    int c[n] = {0, 8, 7, 2, 3, 4, 5};

    int min[m][n];

    for (int i = 0; i <= n; i++)
    {
        min[0][i] = 0;
    }

    for (int i = 1; i <= m; i++)
    {
        for (int j = 1; j <= 0; j++)
        {
            int minN = 10000;
            if (i < c[j])
            {
            }
        }
    }

    return 0;
}